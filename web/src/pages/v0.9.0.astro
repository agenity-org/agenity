---
// v0.9.0 dashboard route — release v0.9.0 of chepherd.
// Mounts the workspace shell (with SpawnWizardV9 wired) + the 4-stage
// Skill-Library-composed spawn flow (#172/#174/#175/#177-180/#192/#194).
//
// URL versioning rule (operator 2026-05-28): each release ships its
// own /vMAJOR.MINOR.PATCH/ route. Minor.patch bumps from here on.
import Workspace from '../components/v08/Workspace.svelte';
import '../styles/global.css';
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>chepherd v0.9.0 — workspace</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <style>
      html, body { margin: 0; padding: 0; height: 100vh; overflow: hidden; background: #0a0a0a; }
      body { font-family: ui-sans-serif, system-ui, sans-serif; color: #f5f5f5; }
    </style>
  </head>
  <body>
    <Workspace client:load version="v0.9.0" />
  </body>
</html>
