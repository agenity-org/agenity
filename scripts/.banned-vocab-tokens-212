# scripts/.banned-vocab-tokens-212 — architect-spec'd #212 sweep tokens.
# Loaded by scripts/banned-vocab-212.sh. Format: one extended-regex pattern
# per line. Blank lines + lines starting with '#' are ignored.
#
# Same convention as .banned-vocab-tokens (the v0.9.1 catalog list);
# both files use leading-'.' so they don't appear in `git ls-files`
# globs that humans typically use to find docs.

Sovereign
Stack[- ]Trio
# `federation` was banned in v0.9.1 rebrand sweep when the prior concept
# was deprecated. v0.9.3 row #225 (C1/C2/C3) re-introduces A2A federation
# as the literal product feature: --federation-registry-url flag,
# internal/federation package, FederatedDeliverer + HostedRegistryDiscoverer
# types, /api/v1/peers cache + announce/poll lifecycle. Token un-banned
# for v0.9.3 since the feature is shipped under exactly this name.
