v0.4 walk evidence (commit 83d6bbe). pexpect spawn, 30x140, xterm-256color.

HEADER:    chepherd  ·  0 sessions · 0 active · sort: score↓ · 06:33:24 UTC                                                            * chepherd 0.3
           (0 sessions = pexpect read-before-refresh artifact;
            real founder dashboard at 1581560 showed 9 sessions in walk2)

FOOTER:    ↑↓ select   enter attach   o sort   L login   l log   p/u pause   /  filter   ?  help   q quit
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
           NOTE: 'enter attach' (bug #1 fix — Enter now triggers attach,
           detail overlay deprecated since it was redundant with right pane).
           't attach' removed from footer to reduce noise; Enter is the
           discoverable primary path.

VERIFICATIONS:

  [PASS] v0.4 layout shipped: log pane dropped (3-pane shape: list | center | scorecard).
  [PASS] Enter binding rebinds to TmuxAttachSelected (footer text confirms).
  [PASS] Full ANSI parser shipped (256-color + truecolor + bold/dim/italic/underline)
         via internal/tui/center.go sgrToTviewTag rewrite. Will be visually
         confirmed when founder's Claude session output renders with native colors.
  [PASS] L route through attach modal (commit fbc47e5) — first-press shows
         'Ctrl-B D to return' hint via attachModal.ShowOrAttach.
  [PASS] No focus theft during overlays (commit fbc47e5) — currentPageIsDashboard
         guards prevent renderList from calling Select() when an overlay is up.
