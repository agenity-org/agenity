{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "chepherd.serviceAccountName" . }}
  labels:
    {{- include "chepherd.labels" . | nindent 4 }}
{{- end }}
